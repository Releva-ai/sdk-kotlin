<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application>
        <!-- BroadcastReceiver for notification action button clicks -->
        <receiver
            android:name=".services.navigation.NotificationActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="ai.releva.sdk.ACTION_NOTIFICATION_BUTTON_CLICKED" />
            </intent-filter>
        </receiver>

        <!-- Transparent trampoline activity for notification actions -->
        <activity
            android:name=".services.navigation.NotificationTrampolineActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:exported="false"
            android:excludeFromRecents="true"
            android:noHistory="true" />

        <!-- Full-screen story viewer activity. Explicit theme because this is an
             AppCompatActivity in a library module: without one, it silently inherits
             whatever theme the host app's <application> tag declares, and a host built on
             plain Material3 (no AppCompat ancestor) crashes with "You need to use a
             Theme.AppCompat theme" the first time this activity opens. -->
        <activity
            android:name=".ui.story.StoryViewerActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:exported="false" />
    </application>

</manifest>
