<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application>
        <!-- BroadcastReceiver for notification action button clicks -->
        <receiver
            android:name=".services.navigation.NotificationActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="ai.releva.sdk.ACTION_NOTIFICATION_BUTTON_CLICKED" />
            </intent-filter>
        </receiver>

        <!-- Transparent trampoline activity for notification actions -->
        <activity
            android:name=".services.navigation.NotificationTrampolineActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:exported="false"
            android:excludeFromRecents="true"
            android:noHistory="true" />
    </application>

</manifest>
